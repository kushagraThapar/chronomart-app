{
  "name": "vector-throughput",
  "durationSeconds": 30,
  "concurrency": 8,
  "rampSeconds": 0,
  "steps": [
    {
      "op": "vectorSearch",
      "container": "ProductVectors",
      "weight": 100,
      "params": { "k": 5 }
    }
  ],
  "_notes": "vectorSearch lands in PR2 — the engine generates a random 1024-dim probe per call."
}
